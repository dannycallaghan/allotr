// import { forwardRef } from 'react';

// // eslint-disable-next-line react/display-name
// export const DatePickerInputElement = forwardRef(({ value, onClick }, ref) => (
//   <button className="example-custom-input" onClick={onClick} ref={ref}>
//     {value}
//   </button>
// ));
